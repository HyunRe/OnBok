# ⚠️ 주의: 이 파일은 절대 Git에 커밋하지 마세요!
# .gitignore에 application-secret.yaml 추가 필수!

spring:
  # MariaDB 비밀번호
  datasource:
    password: 12345678

# Toss Payments API 설정
toss:
  payment:
    # Toss Payments Secret Key (테스트/운영 환경별로 다름)
    # 테스트: test_sk_...
    # 운영: live_sk_...
    secret:
      key: test_sk_DnyRpQWGrNz5YPO0DEAyrKwv1M9E

    # Toss Payments Client Key (프론트엔드용)
    client:
      key: test_ck_KNbdOvk5rk54GqWGbJaz3n07xlzm

# JWT Secret (Spring Security 사용 시)
jwt:
  secret: on_bok_secret_key_1110101010010
  expiration: 86400000  # 24시간 (밀리초)

# OAuth2 설정 (소셜 로그인 사용 시)
spring.security.oauth2.client:
  registration:
    # Google OAuth2
    google:
      client-id: 64400659451-75uj0k262u6e3bv9980c5s37htagek6e.apps.googleusercontent.com
      client-secret: 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
      redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
      scope:
        - email
        - profile

    # Naver OAuth2
    naver:
      client-id: wOVlrDaH7JHFPhv4naKR
      client-secret: xtHCg2xknZ
      client-authentication-method: client_secret_post
      authorization-grant-type: authorization_code
      redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
      scope:
        - name
        - email
        - profile_image
      client-name: Naver

    # GitHub OAuth2
    github:
      client-id: Ov23liu8uD6npbAviUIF
      client-secret: a17ecda146c8dd7e8aa0f6e47166b16ba4a4885a
      redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
      scope:
        - read:user
        - user:email

  provider:
    naver:
      authorization-uri: https://nid.naver.com/oauth2.0/authorize
      token-uri: https://nid.naver.com/oauth2.0/token
      user-info-uri: https://openapi.naver.com/v1/nid/me
      user-name-attribute: response

# AWS S3 설정 (이미지 저장용)
cloud:
  aws:
    credentials:
      access-key: your_aws_access_key
      secret-key: your_aws_secret_key
    region:
      static: ap-northeast-2
    s3:
      bucket: your_s3_bucket_name
